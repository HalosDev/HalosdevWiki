# 🥛 ranks.yml

```yaml
# + ----------------------------------- + #
# | RANKS MODULE                        | #
# | Give spoofed players ranks.         | #
# + ----------------------------------- + #
enabled: true
default_rank: # The default rank to spoof, when module is disabled
  name: default # Name does not have to be a set group name already
  prefix: '&7&lMember &7'
  suffix: ''
ranks: # List of spoofable ranks. Picked at random. Includes default rank. Ordered by priority.
  - name: vip
    prefix: '&c&lVIP &7'
    suffix: '&r &aVery Swag'
```
